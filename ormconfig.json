{
  "type": "sqlite",
  "database": "data/desafio.db",
  "synchronize": false,
  "entities": [
     "dist/models/**/*.js"
  ],
  "migrations": [
     "dist/database/migrations/**/*.js"
  ],
  "cli": {
     "entitiesDir": "src/models",
     "migrationsDir": "src/database/migrations"
  }
}
